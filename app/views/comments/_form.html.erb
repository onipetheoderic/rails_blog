<div class="form-group">
<%= form_for([@post, @post.comments.build]) do |f| %>
	<% if user_signed_in? %>
	<p>
		<%= f.label :name %><br>
		<%= f.text_field :name %>

		<%= f.label :body %><br>
		<%= f.text_area :body %>

		<p><%= f.submit %></p>
	</p>
	<% end %>

<% end %>
</div>